[ข้อมูลเชิงปริมาณและตัวชี้วัดตลาด]
{{quant_json}}

[บทวิเคราะห์และคำบรรยายเชิงคุณภาพ]
{{narrative_json}}

ส่งคืนอ็อบเจกต์ MacroStrategyDirection กรุณาใช้เฉพาะหลักฐาน (Evidence) ที่ปรากฏในข้อมูลนำเข้าเท่านั้น หากสินทรัพย์หลักกลุ่มใดมีหลักฐานสนับสนุนไม่เพียงพอ ให้สร้างมุมมอง Neutral ที่มีความมั่นใจระดับ LOW แทนการสร้างข้อมูลเท็จ บังคับคัดลอกตัวเลขเชิงปริมาณจากคำอธิบาย rationale/thesis ลงในฟิลด์ `supporting_data` เสมอ เพื่อรับประกันหลักฐานอ้างอิงเชิงตัวเลข (Quantitative Provenance) สำหรับมุมมองทองคำ (Gold) ต้องอ้างอิงอัตราผลตอบแทนที่แท้จริง (Real Yields เช่น DFII10) หรือดัชนีดอลลาร์แบบกว้าง (DTWEXBGS จาก FRED ซึ่งแตกต่างจาก DXY) ห้ามสรุปว่าอัตราผลตอบแทนที่แท้จริงที่เป็นบวกในระดับสูงส่งผลบวกต่อทองคำโดยตรง แต่ต้องอธิบายในฐานะปัจจัยกดดันด้านต้นทุนค่าเสียโอกาส (Opportunity-cost headwind) เว้นแต่มีหลักฐานระบุชัดเจนว่า Real Yields กำลังปรับตัวลดลง สำหรับมุมมองเงินสดและตราสารหนี้ระยะสั้น (Cash/T-Bills) ให้ใช้อัตราดอกเบี้ยระยะสั้นและตัวเลขเงินเฟ้อที่ถูกต้อง เช่น Fed Funds Rate, 13-Week T-Bill Yield, CPI, PCE หรือ Core PCE พร้อมเขียนคำอธิบาย rationale เป็นภาษาไทยจากข้อมูลตัวเลขดังกล่าว ห้ามใช้ข้อความสำเร็จรูป 'Backfilled core asset class' เด็ดขาด สำหรับ allocation_delta ห้ามทำซ้ำคำระบุสถานะ ให้ใช้ตัวเลขเปรียบเทียบกับเกณฑ์อ้างอิง เช่น '+3% vs benchmark', '0% vs benchmark' หรือ '-3% vs benchmark' ทุกรายการใน asset_allocation บังคับระบุ `asset_bucket` โดยเลือกจาก ['equities', 'fixed_income', 'commodities', 'fx', 'cash'] ให้ตรงกับหมวดหมู่สินทรัพย์หลักเสมอ ทุกรายการใน `regime_evidence`, สินทรัพย์ และ Pair Trade บังคับต้องระบุ `observable_refs` อย่างน้อย 2 ถึง 3 ID ที่แตกต่างกันจาก valid observables พร้อมระบุ `source_refs` ให้ครบถ้วน หากต้องการอ้างอิงระดับความมั่นใจ MEDIUM หรือ HIGH กำหนดค่า `source_files` และ `source_refs` ของแต่ละรายการจากค่า `source_file` ของตัวชี้วัดที่เลือก (ต้องมีแหล่งอ้างอิงอย่างน้อย 2 แหล่งที่แตกต่างกันต่อสินทรัพย์ หากมีข้อมูล) บังคับให้มีรายการมุมมองสกุลเงิน (Currencies/FX) แยกเฉพาะเสมอ โดยต้องสังเคราะห์ทั้งค่าเงินดอลลาร์ (DXY, EUR/USD จาก Global Snapshot) ร่วมกับอัตราแลกเปลี่ยนค่าเงินบาท (USD/THB), ส่วนต่างอัตราดอกเบี้ยนโยบาย (Policy Differential), ดุลบัญชีเดินสะพัด (Current Account) และภาคการท่องเที่ยวไทย (Tourism context จาก Country/Regional Snapshot) เสมอ เพื่อให้อ้างอิงตัวชี้วัดจากอย่างน้อย 2 แหล่งข้อมูลและหลีกเลี่ยงคำเตือน Single-source ห้ามใช้หุ้นไทยแทนกลุ่มสกุลเงิน FX หากมีการสร้างกลยุทธ์ Pair Trades ห้ามปล่อยฟิลด์ใดว่างเด็ดขาด บังคับระบุค่าตัวเลขที่ชัดเจนใน instrument_proxy (เช่น 'Long QQQ / Short VGK ETF'), hedge_ratio (เช่น '1.0 : 1.0 Notional / Price Ratio'), stop_loss_trigger (เช่น '-3.0% divergence threshold'), target_gain_or_rebalance (เช่น '+6.0% convergence target') และ max_drawdown_limit (เช่น '-4.5% of risk budget') พร้อมระบุ valid observable_refs ให้ครบถ้วน แผนรับมือความเสี่ยง (Risk Scenarios) ห้ามปล่อยฟิลด์ใดว่างเด็ดขาด บังคับระบุเกณฑ์ตัวเลขที่ชัดเจนใน trigger_to_activate (เช่น 'VIX Index > 25.0'), volume_threshold (เช่น 'Daily trading volume > 50,000 contracts'), hedge_size (เช่น '10% of portfolio value') และ unwind_or_cover_condition (เช่น 'VIX Index drops below 18.0') ห้ามใช้คำกำกวมเช่น 'High' หรือ 'Elevated' ห้ามใช้ตัวแทนที่ไม่ผ่านการตรวจสอบ (Unverified proxies) หรือตัวชี้วัดที่ล้าสมัย (Stale indicators) ในการยกระดับความมั่นใจเด็ดขาด **สำคัญมาก (IMPORTANT): กรุณาเขียนคำบรรยายและเหตุผลอธิบายทั้งหมดเป็นภาษาไทยระดับมืออาชีพทางการเงินเท่านั้น (Must output all explanations and free-text fields in Thai language)!**
